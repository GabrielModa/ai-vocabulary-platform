# 027 — Expose local vocabulary generation endpoint

## Goal

Provide a server-only web endpoint that calls Ollama and returns safe validated results.

## Files allowed

`apps/web/**`, workspace lockfile, `tasks/**`.

## Expected commit

`feat(web): expose local vocabulary generation endpoint`
