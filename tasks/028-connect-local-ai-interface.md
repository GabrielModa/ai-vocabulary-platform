# 028 — Connect learner interface to local AI

## Goal

Replace fixed preview content with Ollama output that respects topic, quantity, and CEFR.

## Files allowed

`apps/web/**`, workspace lockfile, `tasks/**`.

## Expected commit

`feat(web): connect capture flow to local AI`
