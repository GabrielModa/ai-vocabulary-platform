# 022 — Build learner capture and review interface

## Goal

Replace the foundation screen with an accessible English-first capture and candidate-review flow.

## Requirements

- Offer typed words, topic plus count, and photo as clear capture modes.
- Offer CEFR A2–C2 and explain that generated candidates remain editable.
- Provide a semantic candidate review state with explicit selection before training.
- Keep one primary action per state and support keyboard/focus/reduced-motion needs.

## Files allowed

`apps/web/**`, `packages/ui/**`, `tasks/**`, learner interface documentation, workspace lockfile.

## Expected commit

`feat(web): build vocabulary capture and review interface`
