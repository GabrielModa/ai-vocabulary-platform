# Docker assets

The root `docker-compose.yml` starts development-only PostgreSQL and Redis services. Production
images will be added with their applications after architecture approval; no placeholder runtime
image is published during the foundation phase.
